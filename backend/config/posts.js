// backend/config/posts.js
const postsConfig = {
  // Maximum number of characters allowed in post content
  // Set to null or 0 to character limit
  characterLimit: 300,
};

module.exports = postsConfig;
